As funcoes usadas no programa sao basicamente para efetuarem calculos matematicos,
sendo elas auto explicativas.

A selecao do algoritmo de ordenacao é feita por meio da variavel "int o",
onde os valores representam os seguintes algoritmos:

1 - Selection Sort;
2 - Insertion Sort;
3 - Shell Sort;
4 - Merge Sort;
5 - Quick Sort;

Para nao haver erros na execucao, deve-se certificar de usar o comando:

"gcc main.c -o "nome_do_arv_objeto" -lm "

Ao incluir p "-lm", ao fim garante que seja importada a biblioteca math.h, usada nos calculos.

Produzido por João Vitor Fernandes Vitor
